import React from 'react';
import DesignTokenGame from './index.jsx';

export default {
  title: 'Tools/DesignTokenGame',
  component: DesignTokenGame,
};

const Template = () => <DesignTokenGame />;

export const Default = Template.bind({});
Default.storyName = 'Design Token Game'; 